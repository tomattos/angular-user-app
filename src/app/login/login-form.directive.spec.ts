import { LoginFormDirective } from './login-form.directive';

describe('LoginFormDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginFormDirective();
    expect(directive).toBeTruthy();
  });
});
